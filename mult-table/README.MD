# Multiplication Table in Go

A small console program written in Go that prints a formatted multiplication table for numbers from 1 to 10.

## What the program does
- Creates an array of numbers from 1 to 10
- Prints a table header
- Outputs a multiplication table with aligned columns

## How to run
```bash
go run main.go
